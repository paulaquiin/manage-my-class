{
  "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently \"npm run dev:server\" \"npm run dev:sass\"",
        "dev:server": "nodemon backend/server.js",
        "dev:sass": "sass --watch public/styles/scss:public/styles/css --no-source-map"
    },
    "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
